import { Color } from "../../models/Color";
import { Combination } from "../../models/Combination";

export class ProposedCombinationView  {
  public async read(): Promise<Color[]> {
    const proposedColors: Color[] = [];

    return proposedColors;
  }
}
